cd /g/node-id-card-app  # Navigate to your project directory
git init                # Initialize the Git repository (if not done already)
git remote add origin https://github.com/Xahidian/Node-ID-Card-App.git  # Add remote repository
git checkout -b feature-branch1  # Create and switch to the feature branch
git add .               # Stage all changes
git commit -m "Initial commit on feature branch"  # Commit the changes
git push -u origin feature-branch  # Push to the feature branch
